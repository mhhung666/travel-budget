# 手機 UI／UX 實際操作檢查

測試日期：2026-09-17。受測程式基準：`21ecc89`；應用版本以 `package.json.version` 為準。

本次使用 Playwright 驅動 Chrome，以手機觸控模式實際開啟與操作本機應用。已完成註冊、建立旅行、記帳、行程日、個人預算與筆記等流程。優先改善的是**個人統計頁橫向溢出**、**手機匯出按鈕缺少可讀名稱**，其次是觸控範圍及搜尋／篩選體驗。

本文件包含初測發現與後續重測；測試作業沒有修改產品程式碼。M01～M07 均已通過各次手機模擬重測；最新 M03／M07 驗證如下。

> 圖片與原始測試數據僅保留於測試工作區，未納入 Git；下列本機證據路徑以 `docs/` 為相對根目錄，不是儲存庫內的可用連結。重現條件、量測結果與驗收結論均記錄於本文。

## M03／M07 第二次修正重測（2026-09-17）

**結果：兩項均通過。** 受測內容為 `2178ec8` 加上目前工作目錄修正，未注入臨時 CSS。沿用獨立本機資料庫，以 Chrome 手機觸控模式測試 320 × 568、390 × 844、412 × 844。

- **M03**：確認粗指標媒體條件成立、Leaflet 樣式載入後，三種尺寸的放大／縮小按鈕均為 **44 × 44px**；實際觸控放大、縮小正常。較高權重的地圖 selector 已解決原先覆蓋問題。
- **M07**：每種尺寸各重複三次「查看 0 筆結果」與「查看 1 筆結果」，共 **18 次流程全數通過**。每次收合後連續觀察 60 個 animation frames，捲動位置均固定：320px 零結果為 `scrollY = 42`，其餘為 `0`。零結果的清除按鈕使用一般 `tap()` 即可操作，未繞過穩定性檢查；有結果時重新展開仍保留餐飲條件，清除正常。頁首進入／退出精簡模式的不同門檻已消除本次重現流程的抖動。
- **相關測試**：`deferredExpenseSearch`、`phase3Experience`、`expenseFilters`、`interactionAccessibility`、`firstUseExperience`，共 5 個測試檔、**41 個測試通過**。
- **驗證界線**：本次為開發伺服器與 Chrome 手機模擬，未驗證 production build、真機 Safari、軟鍵盤及動態網址列。未修改產品程式碼或建立 commit。
- **本機證據**：`evidence/mobile-uiux-2026-09-17/retest-final/results.json` 及同目錄截圖；不納入 Git。

以下保留第一次 P2 重測的失敗數據供比較，現況以本節為準。

## M03～M07 第一次修正重測（2026-09-17）

**結果：M04、M05、M06 通過；M03、M07 部分通過。** M03 的地圖縮放按鈕仍未達 44 × 44px；M07 在 320px 零結果收合時會持續抖動。 受測內容為 `2178ec8` 加上工作目錄尚未提交的 P1／P2 修正。本次沒有修改產品程式碼或建立 commit。

沿用獨立本機 MongoDB、Chrome 手機觸控模擬、繁體中文與一筆 NT$1,280 支出的測試資料，檢查 320 × 568、390 × 844、412 × 844。既有 `deferredExpenseSearch`、`expenseFilters`、`interactionAccessibility`、`firstUseExperience` 共 4 個測試檔、35 個測試全部通過。

| 項目 | 結果 | 實際驗證 |
| --- | --- | --- |
| M03 觸控範圍 | **部分通過** | 三種寬度的次導覽均高 44px；預算入口 96 × 44px，可開啟預算視窗。支出／行程次導覽可切換。地圖縮放仍為 30 × 30px |
| M04 對比 | **通過** | 三種寬度 × 淺／深色，共 6 組次導覽 axe `color-contrast` 掃描均無違規或待人工判定項目；同時涵蓋選取與未選取文字 |
| M05 搜尋清除 | **通過** | 搜尋框「清除搜尋」可恢復結果；同時套用搜尋＋分類後，空狀態「清除搜尋與篩選」一次清空條件、徽章並恢復原支出 |
| M06 登入首屏 | **通過** | 390／412px 的登入按鈕位於 y=628～676，完整出現在 844px 首屏；320px 位於 y=658～706，向下捲動至少 138px 可完整看到 |
| M07 篩選收合 | **部分通過** | 三種寬度選擇餐飲後「查看 1 筆結果」可收合、保留條件並清除；但 320px「查看 0 筆結果」後會發生持續抖動 |

### M03 剩餘問題：Leaflet 的 CSS 蓋掉手機縮放尺寸

- **重現**：新開手機觸控 context，直接進入 `/map`，等待縮放按鈕出現。三種寬度均量到 30 × 30px。
- **排除模擬干擾**：第一次跨頁操作的 context 曾出現粗指標媒體條件未生效；因此另外以全新 context，並透過 CDP 明確啟用觸控後重測。確認 `matchMedia('(pointer: coarse)').matches === true`、`navigator.maxTouchPoints === 1` 時，按鈕仍為 30 × 30px。此結論採用獨立重測數據，不採用最初媒體條件不成立的量測。
- **原因**：`src/app/globals.css` 的 `.leaflet-touch .leaflet-bar a` 設為 44px，但其後載入的 Leaflet CSS 使用相同 selector 設為 30px，後者覆蓋前者。三種寬度的 stylesheet 順序均一致。
- **修正方向**：提高限定於地圖容器的 selector specificity，或確保覆寫規則在 Leaflet 之後套用。例如 `.leaflet-container.leaflet-touch .leaflet-bar a`。在瀏覽器臨時套用此 selector 後，三種寬度均恢復為 44 × 44px；此實驗未寫入產品檔案。可先採這個方向修正，再以實際打包產物確認 CSS 載入順序與尺寸。
- **驗收**：在實際觸控媒體條件成立、地圖樣式完整載入後，放大／縮小兩個按鈕均為至少 44 × 44px，且操作正常。本次為 dev server 重現，production build 仍需另驗。

### M07 剩餘問題：320px 零結果收合後，頁首與捲動位置持續變動

- **重現**：320 × 568，旅程只有一筆餐飲支出 → 展開篩選 → 分類選「購物」→ 點擊「查看 0 筆結果」。
- **實測**：收合後空狀態已顯示，但 `scrollY` 在 **44～50px** 之間反覆變動，連續 20 個 animation frames 都未穩定。重新執行相同流程仍可重現。Playwright 正常 `tap()` 因「element is not stable」逾時；直接對按鈕中心發送觸控事件仍可清除並恢復支出，所以不是清除功能本身失效。
- **原因線索**：`TripSpaceShell.tsx` 使用 `window.scrollY > 48` 切換精簡頁首，高度由 48px 變成 40px；觀察到的捲動範圍反覆跨過此門檻。推測收合後的捲動校正／scroll anchoring 與頁首高度切換形成循環，仍需在修正時確認。390／412px 同流程回到 `scrollY = 0`，正常點擊可通過。
- **建議**：檢查 `showResults()` 的回捲位置，以及頁首縮放門檻；讓收合後停在穩定位置，或為精簡模式加入不同的進入／離開門檻，避免高度變動反覆觸發狀態。
- **驗收**：320 × 568 的 0 筆與有結果兩種情境都能一次收合，頁首與捲動位置穩定；清除按鈕可以一般點擊操作，不需要繞過穩定性檢查。補測未固定視窗高度的手機真機。

### 操作邊界與證據

- M04 掃描範圍是支出頁次導覽，包含選取／未選取狀態；不宣稱全站顏色對比已通過。
- M05 清除後實際焦點不在文字輸入框；本次沒有驗證真機軟鍵盤是否收起。
- M06 在 320px 的標語於標點換行，「足跡」未再被拆開；真機鍵盤與動態網址列仍未驗證。
- M07 採用保留原有展開區、新增結果數與收合按鈕的方式。320px 下按鈕仍需捲動才能找到，但可正常點擊；按下後第一筆結果位於 y=311～373，重新出現在首屏。有結果時已符合原報告允許的「增加收合及結果數提示」方案；零結果的穩定性尚未通過。
- 原始數據：本機 `evidence/mobile-uiux-2026-09-17/retest-p2/results.json`；地圖獨立確認：`map-confirmed.json`；額外操作檢查：`additional.json`。截圖也存於相同目錄，未納入 Git。

## M01／M02 修正後重測（2026-09-17）

**結果：M01、M02 均通過本次手機模擬驗收。** 受測版本為 `21ecc89` 加上工作目錄中的 `StatsDashboard.tsx`、`RuleBasedInsights.tsx`、`ExportMenu.tsx` 修正，並非只測已提交的 HEAD。未使用瀏覽器臨時 CSS 修補。

使用獨立本機資料庫重建測試資料，Chrome 觸控模式、淺色主題，分別測試原始資料及加長旅行名稱／支出描述、金額 NT$1,234,567.89 的資料。每組使用新瀏覽器 context，避免快取混入前一組資料。

| 手機寬度 | 原始資料頁寬 | 長文字／大金額頁寬 | 匯出可讀名稱／axe | CSV 下載 |
| --- | --- | --- | --- | --- |
| 320px | 320px | 320px | 兩組均通過 | 兩組均成功 |
| 390px | 390px | 390px | 兩組均通過 | 兩組均成功 |
| 412px | 412px | 412px | 兩組均通過 | 兩組均成功 |

- **M01**：每組另外切換「分類」與「旅程」統計維度，合計 12 次尺寸檢查，`scrollWidth === clientWidth` 全數成立。原先 457px 的頁面溢出已消失。長名稱會換行，明細會省略，金額仍可見。320px 搭配長名稱及大金額時，旅程分析卡片的名稱欄較窄、卡片較高，屬後續閱讀密度可改善之處，不影響此次溢出修正通過。
- **M02**：6 組均可用 `getByRole('button', { name: '匯出', exact: true })` 找到按鈕，無障礙快照為 `button "匯出"`；axe `button-name` 規則均無違規。實際點擊可開啟 Markdown／CSV／JSON 選單，6 次 CSV 下載均成功，檔案包含對應描述及金額。
- **驗收界線**：本次只針對兩項 P1；axe 重測僅執行 `button-name`，不代表 M04 對比問題或其他無障礙項目已修正。未驗證 Markdown／JSON 檔案內容、真機 Safari、其他語系及任意極端金額。
- **證據**：完整重測數據（本機檔案 `evidence/mobile-uiux-2026-09-17/retest-p1/results.json`）、320px 原始資料（本機檔案 `evidence/mobile-uiux-2026-09-17/retest-p1/original-320-stats.png`）、320px 長文字與大金額（本機檔案 `evidence/mobile-uiux-2026-09-17/retest-p1/long-large-320-trip-dimension.png`）、匯出選單（本機檔案 `evidence/mobile-uiux-2026-09-17/retest-p1/long-large-320-export.png`）。長截圖中的固定頁首／底部導覽位置受捲動位置影響，不視為產品內容重疊。

下列初測數據保留供修正前後比較。

## 測試環境與範圍

- 本機 Next.js 開發伺服器，Chrome headless，`isMobile: true`、`hasTouch: true`、繁體中文。
- 視窗：320 × 568、390 × 844、412 × 844 CSS px。這些是手機尺寸模擬，不等同 iPhone Safari 或 Android 真機驗收。
- 獨立 Docker MongoDB replica set 與測試帳號；未使用既有使用者資料。外部寄信、AI 及檔案儲存服務停用。
- 旅行名稱「東京五天四夜・手機版實際操作測試」，一名成員、一筆 NT$1,280 餐飲支出、一個行程日；另實際新增 NT$10,000 個人預算及一則筆記。
- 三種寬度各檢查 13 個頁面：旅行列表、旅程支出／結算／設定／筆記／清單／相簿、個人統計、我的、個人資料、外觀、旅行地圖、會籍。行程頁及登入／註冊另在操作流程中檢查。
- 39 組頁面／尺寸量測中，3 組個人統計頁出現橫向溢出，其餘 36 組未測到頁面級橫向溢出。
- 同時使用 DOM 尺寸量測與 axe-core 掃描。axe 僅掃描個人統計與支出頁，並非全站無障礙驗收。
- 圖片左下角的黑色「N」是開發工具，不列為產品缺失。長截圖中的固定底部導覽顯示於原視窗位置，不據此認定內容無法捲動。

## 實際操作結果

| 流程 | 結果與範圍 |
| --- | --- |
| 註冊 → 旅行列表 | 成功，以 UI 填寫四個欄位建立測試帳號 |
| 建立旅行 → 旅程首頁 | 成功；名稱可建立，長標題在頁首省略 |
| 底部「記一筆」→ 新增支出 | 成功；填入 1,280、描述、餐飲分類後送出，重新開頁仍有資料 |
| 支出搜尋、展開、匯出及篩選 | 搜尋無結果後清空可恢復；展開可見分攤及編輯入口；已開啟匯出選單與篩選區，未驗證匯出檔案內容 |
| 支出草稿 | 輸入 99 後關閉，顯示「草稿已保留」；未進一步驗證跨裝置或跨瀏覽器保存 |
| 新增行程日 | 成功，填入標題及內文，儲存後出現在時間軸 |
| 設定預算 | 成功，顯示預算 10,000、花費 1,280、剩餘 8,720 |
| 隨手記 | 成功，以 UI 送出一則文字筆記 |
| 外觀 | 成功切換深色，支出頁呈現深色樣式；另觀察到開發環境 hydration 訊息，見待驗證項目 |
| 結算、相簿、清單、地圖、會籍等 | 檢查目前資料下的頁面／空狀態，未宣稱多人還款、照片上傳或新增航班已驗收 |

## 已確認缺失

優先級：P1 為優先修正、影響版面或操作辨識；P2 為接續改善。以下優先級是本次產品評估，不等同掃描工具的 severity。

### M01 · P1 · 個人統計頁被長內容撐寬（重測通過）

- **重現**：建立上述長名稱旅行，新增一筆支出，從底部開啟「個人統計」，使用本月範圍。
- **實測**：320／390／412px 視窗的 `documentElement.scrollWidth` 都是 **457px**，超出各自的 `clientWidth`。統計卡片及底部導覽寬度不一致，右側內容超出手機可視範圍。
- **影響**：閱讀與比較金額需要橫向移動；使用者容易忽略被擠到右側的內容。
- **原因線索**：`src/components/stats/StatsDashboard.tsx` 的統計區塊 grid 子項未限制最小寬度。僅在瀏覽器臨時加入 `section.grid > * { min-width: 0 }`，320px 的 scrollWidth 就回到 320px。這是定位實驗，尚非正式修正或完整回歸驗證。
- **建議**：對相關 grid 子項加入 `min-w-0`／適當的 `minmax(0, 1fr)`，檢查明細中的長旅程名稱與金額排列；不要直接用全頁 `overflow-x: hidden` 掩蓋問題。
- **驗收**：三種手機寬度下，長旅行名、長描述及大金額資料均不造成頁面級橫向捲動；圖表若有局部橫向捲動，僅發生於其容器。
- **證據**：統計長截圖（本機檔案 `evidence/mobile-uiux-2026-09-17/12-stats-overflow.png`）、尺寸量測（本機檔案 `evidence/mobile-uiux-2026-09-17/stats-widths.json`）。

### M02 · P1 · 支出匯出圖示沒有可讀名稱（重測通過）

- **重現**：手機開啟支出頁，搜尋框右側的下載圖示可打開 Markdown／CSV／JSON 選單。
- **實測**：按鈕沒有 accessible name；axe 回報 `button-name`。`ExportMenu.tsx` 將文字在手機隱藏，按鈕未補 `aria-label`。
- **影響**：螢幕閱讀器及依名稱操作的輔助工具無法知道它是「匯出」。純圖示也提高初次使用的辨識成本。
- **建議**：使用在地化的 `aria-label`，隱藏的文字改為 `sr-only` 或保留清楚標示；圖示設為裝飾用途。
- **驗收**：手機無障礙樹中的名稱為「匯出」或「匯出支出」，axe 不再回報該按鈕，點擊仍可選擇匯出格式。
- **證據**：匯出選單（本機檔案 `evidence/mobile-uiux-2026-09-17/15-export-menu.png`）、axe 結果（本機檔案 `evidence/mobile-uiux-2026-09-17/axe-results.json`）。

### M03 · P2 · 次導覽與預算入口的觸控區域偏小（第二次重測通過）

- **重現**：開啟旅程的支出／行程子頁，點擊次導覽或未設預算時的「設定我的預算」。
- **實測**：次導覽高 **24px**；預算入口 **72 × 16px**。地圖放大／縮小按鈕另為 **30 × 30px**。這些數值是實際元素框，不只是文字大小。
- **影響**：單手操作與走動時較難準確點擊；預算入口尤其容易點空。
- **建議**：主要觸控入口採至少 44 × 44px 的產品設計目標，可擴充 padding／點擊範圍；此數值在本報告中是設計目標，不直接據此宣稱違反無障礙規範。相關入口位於 `TripSpaceShell.tsx`。
- **驗收**：三種寬度都達到目標，擴充後的點擊區不與相鄰控制項重疊，次導覽仍能順利切換。
- **證據**：旅程首頁（本機檔案 `evidence/mobile-uiux-2026-09-17/05-trip-created.png`）、下方附錄的頁面量測。

### M04 · P2 · 淺色模式次導覽文字對比略低（重測通過）

- **重現**：淺色模式開啟支出頁，觀察已選取的「支出明細」。
- **實測**：axe 回報前景 `#1b7e74`、背景 `#e8f2f1`、12px 一般字重，對比 **4.29:1**，低於該工具對一般文字採用的 4.5:1 判定。
- **建議**：調深選取狀態文字，保留背景及文字以外的選取提示；同步檢查其他共用次導覽。
- **驗收**：修正後重新掃描淺／深色模式及選取／未選取狀態，文字對比通過，選取狀態仍明確。
- **證據**：axe 結果（本機檔案 `evidence/mobile-uiux-2026-09-17/axe-results.json`）。

## 操作改善建議

以下是依操作觀察提出的設計建議，不視為功能故障。

### M05 · P2 · 搜尋無結果缺少直接恢復入口（重測通過）

- **重現**：支出搜尋輸入「不存在的項目」。畫面說「試著調整或清除篩選條件」，但空狀態沒有清除按鈕，搜尋框也沒有直接清空控制項。
- **影響**：必須重新聚焦輸入框刪除內容，或展開篩選找清除操作，才能回到清單。
- **建議**：空狀態加入「清除搜尋與篩選」，搜尋框加入有可讀名稱的清除按鈕。
- **驗收**：一次點擊恢復原支出清單，搜尋文字、篩選值與數量徽章一致重置，手機鍵盤不必再次彈出。
- **證據**：搜尋無結果（本機檔案 `evidence/mobile-uiux-2026-09-17/17-search-empty.png`）。

### M06 · P2 · 手機登入的主要按鈕太接近首屏底部（重測通過）

- **重現**：未登入，以 390 × 844 開啟首頁。
- **實測**：宣傳內容位於表單之前，登入按鈕約在 y=809～857，首屏無法完整顯示；較短螢幕還需更多捲動。標語中的「足跡」也被斷成兩行。
- **影響**：回訪使用者需要越過較多內容；輸入時的鍵盤影響仍須真機確認。
- **建議**：手機版縮短宣傳區、減少頂部留白，或讓回訪入口更早出現；為中文標語安排較自然的斷行。
- **驗收**：390 × 844 未聚焦輸入框時能完整看到登入按鈕；320 × 568 仍能用少量捲動完成操作，鍵盤情境另做真機驗收。
- **證據**：登入首頁（本機檔案 `evidence/mobile-uiux-2026-09-17/01-home.png`）。

### M07 · P2 · 小螢幕展開篩選後，看不到結果與清除入口（第二次重測通過）

- **重現**：320 × 568 支出頁點擊「篩選」。四個選單與兩個日期欄位依序展開。
- **觀察**：篩選區從約 y=238 延伸到 y=736，清除與結果列表都落在首屏外。它們仍可捲動到，並非被固定導覽永久遮住。
- **影響**：調整條件後，需要上下移動才能確認結果，連續嘗試條件較費力。
- **建議**：手機使用可收合的篩選抽屜，提供明確的「查看結果」及「清除」；或在現有篩選區增加收合及結果數提示。
- **驗收**：選完條件後，一次操作回到結果；清除與關閉容易找到，重新開啟仍保留已選條件。
- **證據**：320px 篩選區（本機檔案 `evidence/mobile-uiux-2026-09-17/16-filters.png`）。

## 建議保留的設計

- 手機新增支出使用滿版表單與固定送出按鈕；320px 下內容可獨立捲動，送出入口仍在底部。
- 新增旅行先呈現名稱與目的地，把日期及描述設為選填展開，首次建立流程較短。
- 支出表單關閉時顯示草稿保留提示；預算儲存後直接顯示剩餘額度，結果容易確認。
- 旅行列表與行程空狀態有明確的首次新增入口。

## 待驗證與限制

- **外觀頁 hydration**：本機直接進入 `/settings/appearance` 時曾出現 React hydration mismatch 開發訊息，元件堆疊包含 `AppearanceSection`。主題依 `useTheme()` 直接決定按鈕 variant，應以乾淨瀏覽器及 production build 再重現；本次不將開發浮層直接視為正式產品版面故障。
- **手機真機**：尚未驗證 iOS Safari／Android Chrome 軟鍵盤、中文輸入、自動放大、瀏海 safe area、動態網址列、旋轉及返回手勢。
- **PWA／離線**：本次使用 dev server，未驗證 service worker、離線重送、安裝或推播。支出送出曾顯示「已暫存支出」，後續新開頁已可讀到該筆；不據此推論離線同步完整通過。
- **多人與大量資料**：僅一名測試成員、一筆支出，尚未驗證多人自訂比例分帳、長成員名、大量清單、還款與權限差異。
- **外部服務**：未驗證 AI 解析、照片／收據上傳、寄信及公開分享完整流程。
- **效能**：開發編譯與測試機因素會影響時間，本次不把首次載入等待秒數列為正式效能缺失。

## 附錄與下一步

M01～M07 均已通過手機模擬重測。後續以 production build 確認樣式，並以至少一台 iPhone 與一台 Android 補驗鍵盤、動態網址列和安全區域。

本機證據位於 `evidence/mobile-uiux-2026-09-17/`。其中 `page-metrics.json` 包含路由、寬度與觸控區域；小於 44px 的候選項目仍需人工判斷，不能將地圖 attribution 等所有連結直接視為缺陷。頁面溢出判斷使用 `scrollWidth > clientWidth`，避免手機模擬下 `innerWidth` 跟著溢出擴大而誤判。
