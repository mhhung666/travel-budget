# 全站表單 UI／UX 檢查

測試日期：2026-09-17。程式基準：`1624c03`；應用版本以 `package.json.version` 為準。

**初測結論：不能判定所有表單符合 UI／UX 要求。** 初次盤點 19 個包含實際 `<form>` 的元件，另納入沒有使用 `<form>` 的行程活動、隨手記、清單、搜尋、分享與設定輸入。已確認 4 項 P1、6 項 P2，主要問題是輸入被清空、送出失敗遺失內容、中文組字誤送出，以及欄位名稱缺失。

這是全站表單盤點、原始碼檢視及主要流程實測，不是全站無障礙認證，也不是每個條件分支的端到端驗收。下方明列實測與僅檢視原始碼的範圍。前一份手機報告 M01～M07 的通過結論，不代表本報告的新項目已通過。

## F01～F10 實作與重測（2026-09-17）

**本報告的 F01～F10 已實作修正，本機重測通過。** 受測內容為 `1624c03` 加上本次工作目錄變更；未建立 commit、未更新應用版本。下方原始發現保留作為修正前的比較，不能用其失敗數據描述目前程式。

| 項目 | 修正 | 重測證據 |
| --- | --- | --- |
| F01 年份 | 獨立保留輸入中的字串，只將合法年份轉成日期；離焦後提示範圍錯誤 | 三尺寸逐字輸入 `2018` 均保留；單元測試涵蓋刪除補回、替換、無效範圍及外部日期重設 |
| F02 草稿 | 隨手記等待成功才清空文字與附件；送出中鎖定編輯並阻擋重複送出，失敗保留；筆記編輯失敗也不再自動關閉 | 三尺寸攔截 POST 後內容仍在，解除攔截重試成功才清空；單元測試另確認附件與重複提交 |
| F03 中文組字 | 隨手記、清單新增／重新命名、標籤排除 composition Enter 與 keyCode 229 | 三尺寸隨手記模擬組字不送出；320px 清單保留輸入；單元測試涵蓋隨手記、清單與標籤 |
| F04 名稱 | 可見 Label 與 input／textarea／combobox 以唯一 id 關聯；複合日期、地點、分享、分帳、匯率、統計幣別等補充名稱 | 三尺寸飛行、住宿、航空會籍、編輯旅行、登記轉帳掃描均無 `label`／`button-name` 違規；320px 筆記編輯、清單重新命名、建立旅行展開與會籍流水也通過。會籍測試改以欄位名稱取得積分／里數 |
| F05 長視窗 | 編輯旅行、登記轉帳、虛擬成員註冊限制為 90dvh 並允許內容捲動 | 320px 編輯旅行／轉帳位於畫面內；虛擬成員註冊由高 716.38px 改為 511.19px，y=28.41；三尺寸註冊／連結切換正常 |
| F06 關閉草稿 | 建立旅行關閉時保留欄位、目的地與好友選擇，重開提示並提供「清除草稿」；成功才重設 | 三尺寸輸入後關閉重開，名稱保留；明確清除後變空。保存範圍是同一頁面已掛載元件，不含重新整理或跨裝置 |
| F07 載入名稱 | 登入／註冊、寄碼／重設保留「處理中」文字，spinner 為裝飾 | 320px 延遲登入回應時 submit button 名稱為「處理中…」 |
| F08 自動填寫 | 登入、新密碼、帳號、暱稱、Email 宣告對應 autocomplete，帳號／Email 關閉自動大寫與拼字修正 | 瀏覽器確認註冊欄位依序為 username／nickname／email／new-password；實際密碼管理器仍待真機驗證 |
| F09 搜尋失敗 | 地點查詢分開顯示失敗與零結果，保留關鍵字並可重試；取消舊查詢並忽略過期回應 | 三尺寸攔截請求出現重試，重試模擬成功後仍保留 `Tokyo`；單元測試涵蓋失敗恢復與過期回應 |
| F10 驗證回饋 | 密碼不一致時聚焦確認欄位，提供欄位錯誤、aria-invalid 與描述關聯；修改後解除錯誤 | 三尺寸確認焦點與 invalid 狀態；單元測試確認錯誤描述與修正後清除 |

### 檢查結果與限制

- 全套 Vitest：**173 個測試檔通過、12 個跳過；1,644 個測試通過、217 個跳過**。跳過項目包括依賴外部環境的整合／live evaluation，不能視為通過。
- 新增 `formInputSafety.test.tsx`、`locationSearchRecovery.test.tsx` 共 **7 個回歸測試**，並強化 `loyaltyEntryDialog.test.tsx` 的名稱查詢。最後局部重跑 3 檔、9 個測試通過。
- `pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm format:check`、`git diff --check` 均通過。
- 名稱掃描無違規的結論限於本次開啟狀態與指定規則；未宣稱全站所有條件分支、對比、鍵盤順序或螢幕閱讀器已完整通過。
- 仍須補 production build、真機 Safari／Android 軟鍵盤及實際輸入法驗證；四語系訊息已補齊，但其他語系尚未逐一做版面實測。附件保留使用模擬上傳回應測試，未操作正式儲存服務。
- 重測腳本與資料保留於本機 `/tmp/tb-form-fixed/`，包含 `results.json`、`verify.json`、`guest.json`、`more.json`、`finish.json`；圖片與原始資料未加入 Git。部分沿用的探索腳本曾因按鈕文案不符而逾時，已以修正 selector 的後續流程完成對應檢查，不將腳本定位失敗列為產品問題。

以下是修正前的檢查紀錄。

## 環境與方法

- Playwright 驅動 Chrome，手機觸控模式；320 × 568、390 × 844、412 × 844 CSS px，繁體中文、淺色。
- 獨立本機 MongoDB 與測試帳號，包含一筆餐飲支出；另建立測試筆記、清單、虛擬成員及會籍帳戶。未使用正式資料。
- 外部寄信、AI、儲存服務停用；網路失敗以瀏覽器攔截請求模擬。未寄送驗證信或呼叫付費 AI。
- 檢查欄位名稱、輸入／清除、空值與錯誤、送出狀態、保留草稿、手機尺寸與鍵盤事件。axe 僅執行 `label`、`button-name`，部分頁面另含 `aria-input-field-name`／`select-name`；零違規只代表該次規則與狀態通過。
- 中文輸入測試是帶 `isComposing: true` 的 Enter 事件；未宣稱已驗證所有實體輸入法。手機軟鍵盤、Safari 動態網址列、深色、其他語系與 production build 尚未驗證。
- 對話框尺寸結論採動畫結束後的獨立量測。初次掃描的動畫中間座標不作為溢出證據。開發工具浮層不列為產品缺失，補測時僅隱藏該浮層。

## 表單覆蓋矩陣

「實測」表示已操作列出的狀態，不代表該功能所有成功／失敗／權限分支都測過。

| 範圍 | 本次檢查 | 結論與限制 |
| --- | --- | --- |
| 登入、註冊 `LoginForm` | 320px 登入輸入、攔截送出、切換註冊、autocomplete | F07、F08；未重做註冊成功流程 |
| 忘記密碼 `ForgotPasswordModal` | 320px 開啟第一步；兩步驟原始碼 | 首步視窗可容納；寄碼／重設成功僅原始碼檢視，載入名稱問題見 F07 |
| 個人資料／Email `ProfileSection` | 三尺寸初始表單名稱掃描；Email 驗證流程原始碼 | 初始欄位名稱掃描通過；未寄送或確認 Email |
| 修改密碼 `SecuritySection` | 三尺寸名稱掃描；320px 密碼不一致錯誤 | F08、F10；未修改密碼 |
| 建立旅行 `CreateTripDialog` | 320px 初始／展開描述日期、關閉重開、地點查詢失敗 | F04、F06、F09 |
| 加入旅行 `JoinTripDialog` | 三尺寸開啟、名稱／版面 | 此次掃描通過；未測錯誤 ID 與加入成功 |
| 編輯旅行 `EditTripDialog` | 三尺寸開啟、名稱／版面 | F04、F05；未修改旅行 |
| 新增虛擬成員 `AddVirtualMemberDialog` | 三尺寸開啟及名稱掃描 | 此次掃描通過；儲存邏輯原始碼檢視 |
| 虛擬成員註冊／連結 `RegisterVirtualMemberDialog`、`LinkExistingMemberDialog` | 三尺寸專屬邀請頁與兩種視窗切換 | 註冊視窗 F05；未完成帳號轉換 |
| 新增／編輯支出 `ExpenseFormSheet` | 三尺寸新增初始表單；金額、日期、分帳、進階欄位原始碼及既有測試 | 初始名稱掃描通過；自訂分帳等延伸標示問題見 F04，未重跑所有幣別／分帳模式 |
| 個人／分類預算 `BudgetDialog` | 320px 開啟與名稱掃描；分類編輯原始碼 | 初始掃描通過；本次未重做儲存 |
| 登記轉帳 `RecordPaymentDialog` | 三尺寸開啟及名稱／版面 | F04、F05；一人成員不足提示有顯示，未實際登記轉帳 |
| 行程日 `ItineraryDayDialog` | 三尺寸開啟、名稱／版面 | 內容區可捲動；地點控制項 F04 |
| 單一行程活動 `ActivityFormDialog`、`ActivityListEditor` | 原始碼檢視 | 時間／類型有名稱；其他文字依賴 placeholder；未操作航班／住宿帶入與附件 |
| AI 行程匯入 `ItineraryImportDialog`、`ImportPreview` | 320px 輸入階段掃描、預覽原始碼、既有 accessibility 測試 | 輸入階段掃描通過；未執行外部解析 |
| 飛行紀錄 `FlightRecordDialog` | 三尺寸開啟、名稱掃描、切換年份、逐字輸入 | F01、F04；未提交航班與同步累積會籍 |
| 住宿紀錄 `StayRecordDialog` | 三尺寸開啟、名稱掃描；共用日期邏輯原始碼 | F04；共用年份問題 F01，未提交住宿 |
| 會籍設定 `LoyaltyAccountDialog` | 三尺寸預設航空表單掃描；320px 實際儲存測試帳戶 | F04；飯店各計畫條件欄位僅原始碼檢視 |
| 會籍流水 `LoyaltyEntryDialog` | 320px 國泰表單開啟／掃描；既有元件測試 | F04；其他航空／飯店分支僅原始碼檢視 |
| 會籍試算 `CxSpEstimatorDialog` | 原始碼檢視 | 與機場／艙等共用標示缺口；未實測試算 |
| 隨手記新增／編輯 `NoteComposer`、`NoteEditDialog` | 三尺寸失敗／IME；320px 新增成功及編輯掃描 | F02、F03、F04 |
| 清單新增／重新命名 `NewChecklistSheet`、`ChecklistCard` | 320px 建立空白清單、IME 新增項目、重新命名掃描 | F03、F04；跨旅行複製僅原始碼檢視 |
| 支出留言 `ExpenseComments` | 原始碼檢視 | 成功才清空，優於隨手記；欄位仍依賴 placeholder，未實測留言 |
| 支出篩選、統計日期／幣別、旅行匯率 | 原始碼檢視；篩選沿用前份報告實測 | 支出篩選與統計日期已有名稱；匯率及部分幣別控制項見 F04 |
| 相片說明／行程關聯／地點、附件與頭像 | `PhotoLightbox`、`PhotoPinDialog`、上傳元件原始碼檢視 | 相片說明、行程選擇有 aria-label；外部上傳成功／失敗與圖片裁切未實測 |
| 分享欄位 | 旅行／相簿／地圖／成就／年度回顧原始碼檢視 | 複製按鈕有名稱，唯讀連結輸入框缺少標籤，見 F04；未建立公開分享 |
| 外觀、通知、好友與成員選擇 | 開關／選擇元件原始碼盤點 | 不屬自由文字表單；未宣稱全部偏好與權限組合驗收完成 |

## 已確認問題

優先級為產品改善順序，不等同 axe severity。P1 優先處理輸入阻塞、資料遺失、誤送出與核心欄位無法辨識；P2 為其餘操作與回饋改善。

### F01 · P1 · 年份欄位不能逐字輸入

- **重現**：旅行成就 → 新增飛行紀錄 → 日期精度選「年份」→ 清空 → 逐字輸入 `2018`。
- **實測**：三種尺寸的最終值都是空字串。第一個字尚未滿四碼就被清除，後續字元亦同；一次貼上四碼與逐字輸入不是相同驗證。
- **原因**：[RecordFormFields.tsx](../src/components/collections/RecordFormFields.tsx) 的 `DatePrecisionInput` 每次 onChange 只接受四碼，否則把受控值設成空字串。住宿也使用此元件，屬同一修正範圍。
- **改善**：保留可編輯的年份字串，允許空值及 1～4 碼中間狀態；在離焦／送出時驗證年份與轉換日期。
- **驗收**：逐字輸入、全選替換、刪除一碼再補回、貼上四碼均正常；超出範圍時顯示明確原因。飛行及住宿各重測。

### F02 · P1 · 隨手記送出失敗會遺失草稿

- **重現**：隨手記輸入內容，攔截本機 POST 使其失敗，再點「送出」。
- **實測**：三種尺寸均出現錯誤提示 `Failed to fetch`，但輸入框已清空，列表沒有新增內容，也沒有恢復入口。
- **原因**：[NoteComposer.tsx](../src/components/trips/detail/notes/NoteComposer.tsx) 在請求成功前執行 `setDraft('')`、`setAttachments([])`，呼叫端只顯示錯誤，沒有還原輸入。文字遺失已實測；附件同樣提前清空是原始碼發現，未做上傳實測。
- **改善**：成功後才清空；或保留獨立的待送出快照，失敗可恢復／重試，同時避免覆蓋使用者送出後新寫的內容。錯誤使用本地化文字。
- **驗收**：離線、請求失敗、伺服器拒絕時文字與附件仍可重試；成功清空且只新增一次。

### F03 · P1 · 中文組字中的 Enter 被當成送出

- **重現**：填入隨手記文字，觸發 `isComposing: true` 的 Enter；320px 清單新增項目做相同測試。
- **實測**：三尺寸隨手記均觸發送出並清空；清單新增一筆「中文選字中」，輸入亦清空。前者攔截網路避免誤存，後者在本機資料庫確認新增。
- **原因**：[NoteComposer.tsx](../src/components/trips/detail/notes/NoteComposer.tsx)、[ChecklistCard.tsx](../src/components/trips/detail/checklist/ChecklistCard.tsx) 的 Enter handler 沒有排除 composition。共用 [tag-input.tsx](../src/components/ui/tag-input.tsx) 的 Enter／逗號提交也有相同程式碼缺口，尚未做實際標籤操作確認。
- **改善**：組字期間不提交，統一處理輸入法事件；清單重新命名與標籤同步檢查。提交函式也應檢查 pending，避免只停用按鈕卻仍可用鍵盤重複送出。
- **驗收**：注音／拼音／日文選字 Enter 不送出；結束組字後的明確送出才生效；一般 Enter、Shift+Enter 與按鈕仍符合原設計。須補真機輸入法。

### F04 · P1 · 多個欄位有可見文字，但沒有可讀欄位名稱

`<Label>` 與欄位相鄰不等於建立關聯。多個 select／combobox 的選中值也不能代替「航空公司」「出發機場」「收款人」這類用途名稱。

| 實測狀態 | axe 結果（每個尺寸） |
| --- | --- |
| 飛行初始表單，三尺寸 | 6 個 `button-name`、2 個 `label` |
| 住宿初始表單，三尺寸 | 4 個 `button-name`、3 個 `label` |
| 航空會籍設定初始表單，三尺寸 | 2 個 `button-name`、3 個 `label` |
| 登記轉帳，三尺寸 | 付款人／收款人共 2 個 `button-name` |
| 編輯旅行、行程日，三尺寸 | 各 1 個地點 `button-name` |
| 建立旅行，320px 初始及展開 | 地點 1 個 `button-name` |
| 編輯筆記、清單重新命名，320px | 各 1 個 `label` |
| 國泰會籍流水，320px | 1 個 `button-name`、2 個 `label` |

- **涉及元件**：[RecordFormFields.tsx](../src/components/collections/RecordFormFields.tsx)、四個 collections 表單、航空／機場／品牌 combobox、[LocationAutocomplete.tsx](../src/components/location/LocationAutocomplete.tsx)、[RecordPaymentDialog.tsx](../src/components/settlement/RecordPaymentDialog.tsx)、筆記與清單編輯。
- **原始碼延伸範圍**：自訂分帳數字、旅行匯率、部分統計／結算幣別、會籍試算，以及五種分享唯讀輸入框也缺少明確關聯。這些延伸項目尚未逐一跑 axe。placeholder 可能讓工具不報錯，但填入後看不到用途提示，仍應檢查持續可見標籤。
- **改善**：一般欄位使用唯一 id 與 `htmlFor`；共用複合欄位接受 id／label props；日期精度與日期值各有名稱；重複欄位包含成員或幣別；提示與錯誤透過 `aria-describedby` 關聯。
- **驗收**：不靠 placeholder 或目前選值也能辨認欄位用途；點可見標籤能聚焦對應欄位；新增／編輯／條件展開狀態均重測。不能只用新增 aria-label 掩蓋可見標籤不一致。

### F05 · P2 · 小螢幕的長對話框超出畫面

動畫結束後，320 × 568 的量測：

| 視窗 | 上緣 y | 高度 | 下緣 y | 影響 |
| --- | --- | --- | --- | --- |
| 編輯旅行 | -33.59 | 635.19 | 601.59 | 標題及關閉按鈕被裁切 |
| 登記轉帳（只有一名成員，含錯誤提示） | -22 | 612 | 590 | 關閉按鈕部分超出上緣 |
| 虛擬成員註冊 | -74.19 | 716.38 | 642.19 | 標題、關閉與底部取消超出視窗 |

- **界線**：不表示所有儲存按鈕均不可按；編輯旅行的儲存仍在畫面內。390／412 × 844 的上述視窗可容納。虛擬成員連結視窗在三尺寸也可容納。
- **原因**：[dialog.tsx](../src/components/ui/dialog.tsx) 預設置中；上述呼叫端沒有高度限制與內部捲動。行程日、預算已有不同處理，不能一概判定所有 Dialog 有問題。
- **改善**：長表單使用可捲動內容區與可見操作列，或套用既有 `ResponsiveFormSheet`；錯誤訊息增加高度後也應維持可操作。
- **驗收**：320 × 568 及更短可視區域，標題、關閉、欄位及取消／送出都可抵達；補驗實機鍵盤。僅用 `100dvh` 不能取代實機鍵盤測試。

### F06 · P2 · 關閉建立旅行時無提示清除內容

- **重現**：320px 建立旅行 → 輸入「未儲存旅程」→ 點關閉 → 重開。
- **實測**：名稱變成空字串，無保留或放棄確認。來源為 [CreateTripDialog.tsx](../src/components/trips/CreateTripDialog.tsx) 的 `handleClose()`。
- **改善**：有變更時保留草稿或詢問是否放棄；與已有草稿機制的支出表單保持一致。其他長表單也應訂定一致策略，本次不把未實測者一律視為已確認遺失。
- **驗收**：無變更直接關閉；有變更時可恢復或明確放棄；送出成功才清空。

### F07 · P2 · 登入載入中只剩圖示，按鈕名稱消失

- **重現**：320px 登入，延遲 POST 回應，在送出中讀取 submit button。
- **實測**：按鈕文字為空，沒有 aria-label。停用狀態不等於提供「登入中」進度名稱。
- **來源**：[LoginForm.tsx](../src/components/login/LoginForm.tsx) 將整個文字替換為 Loader2；註冊共用同一分支。[ForgotPasswordModal.tsx](../src/components/login/ForgotPasswordModal.tsx) 的寄碼／重設按鈕亦有相同模式，屬原始碼確認。
- **改善／驗收**：保留可讀的「登入中／註冊中／傳送中」文字或名稱，圖示設為裝飾；載入期間仍能取得正確名稱及避免重複提交。

### F08 · P2 · 帳號與密碼欄位未宣告自動填寫用途

- **實測／原始碼**：登入／註冊欄位 autocomplete 皆空；修改密碼、虛擬成員註冊／連結亦未宣告用途。Email 驗證碼已有 `one-time-code`，可保留。
- **影響**：瀏覽器與密碼管理器只能猜測目前密碼／新密碼、帳號及 Email；未宣稱所有瀏覽器的自動填入都已失效。
- **改善**：依情境設定 `username`、`current-password`、`new-password`、`email`；帳號依產品規則關閉不必要的自動大寫與拼字修正。
- **驗收**：真機密碼管理器能辨認登入與設定新密碼；不能將新密碼誤填為目前密碼。密碼顯示／隱藏可另作易用性改善，不列成本次必修失敗。

### F09 · P2 · 地點查詢失敗被顯示成「找不到地點」

- **重現**：320px 建立旅行 → 開啟目的地搜尋 → 攔截 Nominatim 請求使其失敗 → 輸入 `Tokyo`。
- **實測**：畫面顯示「找不到地點」，沒有服務失敗與重試提示。
- **原因**：[LocationAutocomplete.tsx](../src/components/location/LocationAutocomplete.tsx) catch 回傳空陣列，與成功但零結果共用呈現。
- **改善／驗收**：分開處理載入中、零結果、請求失敗；失敗保留關鍵字並可重試；不得暗示地點不存在。搜尋競速／過期回應另應補測，本次未列為已實測問題。

### F10 · P2 · 驗證錯誤未連到需要修改的欄位

- **重現**：320px 修改密碼 → 新密碼與確認密碼不同 → 送出。
- **實測**：有 `role=alert` 顯示「新密碼與確認密碼不符」，但確認密碼未設 `aria-invalid`／`aria-describedby`，焦點也未移至錯誤欄位。
- **改善**：保留摘要提示，同時在確認密碼旁標示原因並建立關聯；送出後聚焦第一個錯誤欄位。其他表單應依實際驗證規則採用相同模式，不能把所有網路錯誤都標到單一欄位。
- **驗收**：錯誤可被看見、讀出並直接修正，輸入不丟失；修正後錯誤狀態解除。AI 匯入預覽已有部分 `aria-invalid`，可作為一致性整理起點。

## 已有良好設計與待補驗

- 共用 Input 預設手機字級 16px、最小高度 44px；Button 預設最小高度 44px。不能僅看到 `h-8` 就斷言實際高度 32px，必須一起看 min-height 與實測。
- 新增支出初始表單在三尺寸名稱掃描均通過；金額有 decimal inputMode，付款人與日期有名稱，支出也已有草稿保留。這不涵蓋所有自訂分帳與進階欄位。
- 個人資料、修改密碼初始名稱掃描均通過；加入旅行與新增虛擬成員亦通過本次名稱掃描。修改密碼的錯誤關聯另有 F10。
- 建立旅行、飛行、住宿、會籍與筆記編輯使用手機 Sheet；行程日有可捲動內容；預算及 AI 匯入在 320px 初始視窗內可容納。
- 部分搜尋／標籤／留言／統計日期覆寫為 12～14px。可能影響手機閱讀與 Safari 聚焦縮放，**尚未實機驗證，不列為已確認縮放故障**。
- 仍需補驗：iPhone／Android 軟鍵盤與輸入法、動態網址列、200% 文字縮放、深色與其他語系、長成員名、多人成員分帳、所有會籍分支、真實寄信／上傳／AI 失敗處理，以及完整 Tab／螢幕閱讀器操作。

## 測試與證據

執行下列既有測試，**7 個檔案、24 個測試全數通過**：

- `expenseDraftForm.test.tsx`、`expenseFormLayout.test.tsx`
- `phase4Accessibility.test.tsx`、`phase4Flows.test.tsx`
- `itineraryImportAccessibility.test.tsx`
- `formQueryReadiness.test.tsx`、`loyaltyEntryDialog.test.tsx`

這些測試未完整涵蓋本次 F01～F10，因此通過不等於全站表單通過。修正後應針對年份逐字輸入、composition Enter、失敗保留草稿及條件欄位名稱增加有意義的回歸測試。

原始腳本、量測 JSON、日誌與截圖保留於本機 `/tmp/tb-form-review/`：`results.json`、`extra.json`、`more.json`、`finish.json`、`guest.json`、`tests.log`。它們不是儲存庫附件；圖片與原始資料未納入 commit。初次檢查只新增報告與文件索引；後續已依上述實作節修正產品程式碼，仍未建立 commit 或 push。

F01～F10 的已確認問題已完成修正；後續依矩陣補齊真機與未測條件分支，才能評估「所有表單符合」是否成立。
