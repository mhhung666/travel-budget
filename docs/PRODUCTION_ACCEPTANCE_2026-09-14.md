# S 正式站驗收（2026-09-14）

## 結論

**部分通過，S 正式站驗收仍未結案。** 待同步支出的離線重載、恢復補送、回應遺失重送、
雙分頁保存、儲存失敗處理、缺少一般快取時的草稿復原均通過；兩筆支出永久失敗後，
快速切回離線並重載，曾出現列表合計 NT$169、頁首摘要 NT$212 的不一致。
連線恢復並更新快取後，再次離線重載為正確的 NT$169；需釐清失敗處理與快取保存時序。

目標為 `https://budget.mhhung.com`，使用者手動登入獨立 Chrome 視窗。
測試集中於新建、只有登入者自己的「S 離線驗收 20260914」旅程，路徑 `/trips/7c8nwx1o/expenses`。
既有旅程沒有寫入；沒有邀請其他成員、操作正式資料庫、執行 migration 或部署。
未取得正式部署 commit，因此不能斷言正式站已包含本機最新修正，亦不能把本次現象直接歸因於最新 commit。

## 結果

| 情境 | 結果 | 觀測與界線 |
| --- | --- | --- |
| 待同步支出立即與再次離線重載 | 通過 | NT$17 的工作在兩次重載後仍為 pending，恢復連線後為 done，列表只有一筆 |
| 伺服器 action 回應遺失 | 通過 | 讓真正 action 執行並回傳 HTTP 200，丟棄回應後離線重載；同一請求補送後只有一筆 NT$19 |
| IndexedDB 儲存失敗 | 通過 | 暫時讓寄送紀錄的 put 拋出 QuotaExceededError，表單輸入保留且沒有 action 請求；恢復儲存後成功新增 NT$23 |
| 兩分頁同時離線新增 | 通過 | NT$29 與 NT$31 皆保留，兩頁重載後均顯示兩筆及正確的 NT$132 摘要，恢復連線後各只有一筆 |
| 一般讀取快取缺失 | 通過 | 刪除驗收瀏覽器的查詢快取後離線重載，顯示資料未保存提示；獨立 NT$37 草稿仍可查看並在連線恢復後同步 |
| 線上填表後切換離線 | 通過複驗 | 表單及未送出的文字保留；最初工具斷開 CDP 網路控制所見的表單消失，不列為應用缺陷 |
| 兩筆永久失敗後的離線摘要 | 未通過一次時序驗證 | 列表 NT$169、摘要 NT$212，多出第二筆失敗金額 NT$43；連線刷新後再次離線為 NT$169，尚未建立穩定最小重現 |
| 失敗草稿保留、匯出、修改重送 | 通過 | 兩筆失敗草稿皆保留伺服器錯誤，可匯出 JSON，修改後分別新增 NT$41 與 NT$43，舊稿退休 |

永久失敗情境以 NT$41／NT$43 的真實離線新增為起點，僅在驗收瀏覽器 IndexedDB 將兩筆待送輸入金額
改為負值，再離線重載與恢復連線，由正式伺服器實際回傳「金額必須大於 0」。
這是人工注入不合法草稿的驗證，未以撤銷成員權限重跑本機原始情境；原先暫時計算上下文保留。
摘要異常發生於兩筆已 failed、線上畫面已見 NT$169 後，立即切離線並重載的階段。
目前證據不足以區分部署版本差異、一般快取保存時序或還原邏輯問題。

## 最終狀態與證據

不帶既有查詢快取、停用 Service Worker 的新瀏覽器 context 重新讀取正式站，確認九筆不同測試支出，
每個名稱恰好一筆，頁首摘要 NT$253；驗收視窗寄送紀錄沒有 pending／failed。
筆數依正式頁面重新載入結果核對，沒有直接查詢正式 MongoDB 或去重 collection。
測試旅程與九筆支出保留供複查，會列入該帳號統計；未刪除或封存。

本機證據位於 `/tmp/budget-production-s-20260914/`：

- `failed-case-error.png`／`.txt`：摘要 NT$212、列表 NT$169。
- `09-failed-recheck.png`：連線刷新後再次離線為 NT$169。
- `09-draft-export.png`、`synthetic-failed-draft.json`：失敗草稿及匯出。
- `11-fresh-server.png`／`.txt`：新瀏覽器重新讀取的最終九筆與 NT$253。
- 其餘截圖與 `results-more.json`、`results-last.json` 保存已通過情境；`results-failed.json` 保存未通過斷言。

暫存證據不保證永久保留。本次未執行 iOS Safari／安裝 PWA、正式 DB 索引與 migration 核對、
同頁兩個 hook 同時新增的程式級競態、效能量測或 M／O／P／R 的完整驗收。
